---
title: "关于我们"
subtitle: "“这有光”在做什么？我们的内容有什么特点？"
format:
  html
page-layout: full
toc: false
anchor-sections: false
editor: source
---

## “这有光”是谁？

```{python}
#| label: fig-polar
#| fig-cap: "A line plot on a polar axis"

import numpy as np
import matplotlib.pyplot as plt

r = np.arange(0, 2, 0.01)
theta = 2 * np.pi * r
fig, ax = plt.subplots(
  subplot_kw = {'projection': 'polar'} 
)
ax.plot(theta, r)
ax.set_rticks([0.5, 1, 1.5, 2])
ax.grid(True)
plt.show()
```